server:
  port: 8081

spring:
  application:
    name: CHURNINSIGHT_ONE_BK_J17

  datasource:
    url: jdbc:postgresql://localhost:5432/churninsight_db
    username: postgres
    password: 12345
    driver-class-name: org.postgresql.Driver

security:
  jwt:
    key:
      private: ClaveDeSecretoSuperSecretosaParaElDesarrollo12345
    user:
      generator: churninsight_backend

logging:
  level:
    org:
      springframework:
        security: DEBUG

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update 
    show-sql: true
    properties:
      hibernate:
        format_sql: true

#   sql:
#     init:
#       platform: postgres

# logging:
#   level:
#     org:
#       hibernate:
#         SQL: DEBUG
#         type:
#           descriptor:
#             sql:
#               BasicBinder: TRACE